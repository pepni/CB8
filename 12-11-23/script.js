//console.log("ciao");

//esercizio 1

const titleEl = document.querySelector(".title");

const heroGenerator = (heroImg, heroText, heroBtn) => {
  const hero = document.createElement("hero");
  const heroImg = document.createElement("hero-img");
  const heroText = document.createElement("hero-text");
  const heroBtn = document.createElement("hero-button");

  hero.setAttribute("class", hero);
  heroImg.setAttribute("src", heroes.png);
  heroPic.setAttribute("alt", heroPic);
  heroText.setAttribute("hero-text", h1, h2);
  heroBtn.setAttribute("hero-button", heroBtn);

  //non riesco a inserire una png dalla cartella

  let heroPic = document.getElementById("heroes.png");

  return hero;
};

document.hero.append(heroGenerator(heroImg, heroPic, heroText, heroBtn));

//non riesco a collegare css e js
